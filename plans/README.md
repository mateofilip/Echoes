# Animation Plans — FirefoxNotice

Commit: `f8641a4`
Generated by `improve-animations` — read-only audit, no source modified.

## Findings (vetted, by leverage)

| # | Severity | Category | Location | Finding | Fix summary |
|---|----------|----------|----------|---------|-------------|
| 1 | HIGH | Performance | `FirefoxNotice.tsx:46` | `backdrop-blur-md` on full-viewport overlay + `filter: blur` on modal doubles GPU cost, Firefox falls back to CPU | Remove overlay blur, fade opacity only |
| 2 | MEDIUM | Purpose & density | `FirefoxNotice.tsx:91` | `mt-3 space-y-4` + 2 paragraphs ≈35 words feels heavy for transient notice | Cut to 1+1 lines, `mt-2`/`mt-3`/`mt-5`, ≤25w |
| 3 | HIGH | Performance/Easing | `FirefoxNotice.tsx:55` | `filter: blur(10px)` + `y/scale` shorthands 500ms spring > budget, main-thread | `transform` + `opacity` 200ms `ease-out` |

## Plans

| N | Title | Severity | Status | Depends |
|---|-------|----------|--------|---------|
| 001 | Remove backdrop blur from Firefox overlay | HIGH | TODO | — |
| 002 | Reduce Firefox modal text density and spacing | MEDIUM | TODO | — |
| 003 | Simplify Firefox modal to transform+opacity 200ms | HIGH | TODO | 001 (do overlay first) |

Recommended order: 001 → 003 → 002 (001+003 are performance, 002 is density).

## How to execute

```
improve-animations execute 001
improve-animations execute 003
improve-animations execute 002
```

Each plan is self-contained — executor needs no prior context, values are inlined from AUDIT.md.
